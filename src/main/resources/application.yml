spring:
  profiles:
    default: local

  datasource:
    url: jdbc:mariadb://localhost:3306/trecord
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: none # Do not change
    open-in-view: false

feign:
  client.config.default:
    connectTimeout: 1000
    readTimeout: 3000
    loggerLevel: HEADERS

jwt:
  secret-key: "adZw12pxc55CmnIKsNP42KIa+M+bTlezcJKs3sW2RctO"
  token-expired-time-ms: 86400000

google:
  client-id: "This is secret value"
  client-secret: "This is secret value"
  oauth2.endpoint: "https://oauth2.googleapis.com"
  api.baseUrl: "https://www.googleapis.com"

logging:
  level:
    world.trecord.web.client.feign.client: DEBUG
---
spring:
  config:
    activate:
      on-profile: local

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

---
spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

google:
  oauth2.endpoint: "http://localhost:8089"
  api.baseUrl: "http://localhost:8089"