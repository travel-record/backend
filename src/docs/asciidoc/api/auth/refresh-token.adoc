[[refresh-token]]
===== HTTP Request

include::{snippets}/refresh-token/http-request.adoc[]

===== Notes

- 이 API는 사용자가 토큰을 재발급할 때 사용합니다
- 요청 바디에는 리프레시 토큰이 포함되어 있어야 합니다.

===== HTTP Response

include::{snippets}/refresh-token/http-response.adoc[]

===== Notes

- 응답에는 재발급된 토큰 및 리프레시 토큰이 포함됩니다.
- 유효하지 않은 토큰으로 요청하면 오류가 발생합니다.

===== Potential Errors

- `INVALID_TOKEN`: 유효하지 않은 토큰으로 요청한 경우

include::{snippets}/refresh-token/response-fields.adoc[]
